 trigger:
 - master

 pool:
   vmImage: 'ubuntu-latest'

 jobs:
   - job: maven_build
     steps:
      - script:
          mvn compile
          
     job: maven_test
     steps:
      - script:
          mvn test
          testresults: '**TEST*.xml

     job: maven_package
     steps:
      - script:
          mvn package
          warfile:  '**/target/addressbook.war
