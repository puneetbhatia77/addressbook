 trigger:
 - master

 pool:
   vmImage: ubuntu-latest


 steps: 
       - task: CmdLine@2
  inputs:
    script: |
      apt update
      apt install openjdk-11-jdk -y
      apt install maven -y
      mvn compile
      mvn test
      testresults:'**TEST*.xml      
      mvn package
      warfile:'**/target/addressbook.  
